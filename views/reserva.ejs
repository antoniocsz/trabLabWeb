<% include header.ejs %>
<% include navbar.ejs %>
<% include sidebar.ejs %>
<h3></h3>

 <div class="row">
    <div class="col m3 "></div>
    <div class="col s12 m6 ">
        <div class="card z-depth-5">
        
        <div class="card-content">
        <span class="card-title">Reserva</span>
        <a href="#" class="waves-effect waves-light btn blue darken-1 right">Novo Reserva</a>
        <table class="striped">
            <thead>
                <tr>
                    <th>Sigla</th>
                    <th>Apelido</th>
                    <th>Posto</th>
                    <th>Qtd</th>
                    <th>Data</th>
                </tr>
            </thead>
            <tbody>
            <% for(var i=0; i< reservas.length; i++) {%>
                <tr>
                    <td><%= reservas[i].sigla %></td>
                    <td><%= reservas[i].nomeguerra %></td>
                    <td><%= reservas[i].posto %></td>                    
                    <td><%= reservas[i].quantidade %></td>
                    <td><%= reservas[i].data %></td>
                </tr>
            <% } %>
            </tbody>
            </table>
        </div>
        
        </div>
    </div>

    <div class="col m3 "></div>
</div>

<% include footer.ejs %>
